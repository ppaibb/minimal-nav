{
  "name": "minimal-nav",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Nav - High Performance Minimalist Navigation Platform",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:bin": "npm run build --prefix frontend && go run scripts/build.go",
    "build:all": "npm run build --prefix frontend"
  }
}
